{
  "name": "pixelquota",
  "version": "1.3.3",
  "description": "Privacy-first image compressor for exact KB and pixel limits. Runs entirely in the browser.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/build.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test:e2e": "node tests/e2e.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "heic2any": "^0.0.4"
  },
  "devDependencies": {
    "puppeteer-core": "^25.11.0",
    "vite": "^7.1.5"
  }
}
